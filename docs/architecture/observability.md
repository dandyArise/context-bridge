# Observability

LM Studio receives blue/red context content blocks with before/after tokens, reserve, reused chunks,
archive location, and actionable failures. Debug logs record control-flow errors but never prompt
content, API keys, response bodies, or authorization headers. There is no telemetry, analytics, or
remote logging.
