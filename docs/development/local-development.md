# Local development

Use Node.js 20+, pnpm, and an installed LM Studio CLI. Run `pnpm install`, `pnpm run verify`, then
`lms dev --no-notify`. LM Studio controls native configuration-field rendering.
